services:
  vault-tracker:
    image: ghcr.io/adamdevlpmnt/vault-tracker:beta
    container_name: vault-tracker
    restart: unless-stopped
    volumes:
      - /mnt/TANK/Config/Vault-Tracker:/data
    environment:
      QB_HOST: "http://192.168.1.100"
      QB_PORT: "8080"
      QB_USERNAME: "admin"
      QB_PASSWORD: "adminadmin"
      POLL_INTERVAL: "10"
      RETRY_DELAY: "30"
      MAX_RETRIES: "0"
      DB_PATH: "/data/vault-tracker.db"
      LOG_LEVEL: "INFO"
      MIN_SIZE_BYTES: "4294967296"  # 4 GB — only process torrents larger than this
